# Server
PORT=3000
ENABLE_SWAGGER=true
SWAGGER_PASSWORD=your_secure_swagger_password
# Comma-separated list of allowed CORS origins (REST API). Required for browser clients with credentials (cookies).
ALLOWED_ORIGINS=http://localhost:3000,https://yourapp.com
# Socket.IO allowed origin (must match ALLOWED_ORIGINS in production)
SOCKET_ORIGIN=http://localhost:3000

# MongoDB
MONGO_URI=mongodb://localhost:27017/play4cash

# Redis
REDIS_URI=redis://localhost:6379

# JWT Auth
JWT_SECRET_KEY=your_jwt_secret_key
JWT_ACCESS_TOKEN_TTL_SECS=3600
JWT_REFRESH_TOKEN_TTL_SECS=86400
JWT_ISSUER=play4cash-api
JWT_AUDIENCE=play4cash-clients

# HttpOnly refresh cookie (browser / PWA). Use SAMESITE=none + SECURE=true when SPA and API are on different sites.
AUTH_REFRESH_COOKIE_NAME=p4c_refresh
AUTH_REFRESH_COOKIE_SAMESITE=lax
AUTH_REFRESH_COOKIE_SECURE=false

# Email
EMAIL_SERVICE=gmail
EMAIL_FROM=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_VERIFICATION_EXPIRY_MINUTES=60

# Binance API
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_api_secret

# Withdrawals / Transactions
PROCESSING_EXPIRY_MINUTES=30
WITHDRAWAL_VERIFICATION_EXPIRY_MINUTES=30
MIN_WITHDRAWAL=10
JOB_CRON_WITHDRAWAL_IN_PROCESSING="*/5 * * * *"
GOOGLE_CLIENT_ID=web_client

# Admin (comma-separated)
ADMIN_EMAILS=admin@example.com

# Gateway trust (optional). If unset, client-supplied x-gateway-user is always removed.
# When using Play4CashGateway: set the same secret on gateway and API.
GATEWAY_INTERNAL_SECRET=
GATEWAY_TRUST_HEADER_NAME=x-gateway-internal
TRUSTED_GATEWAY_IPS=
